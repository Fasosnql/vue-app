import {ActionTree} from 'vuex';
import mutationTypes from './mutation-types.ts';
import {State} from './state.ts';

const actions: ActionTree<State, State> = {
  //TODO actions
};

export default actions;